# 📘 VS Code + GitHub 사용 가이드

> 초보자를 위한 나만의 Git/GitHub 사용법 정리 노트

---

## 1. 기본 개념

| 용어 | 의미 |
|---|---|
| **Git** | 파일 변경 이력을 관리해주는 프로그램 (내 컴퓨터에서 동작) |
| **GitHub** | Git으로 관리한 프로젝트를 인터넷에 올려두는 저장소 서비스 |
| **저장소(Repository)** | 프로젝트 하나를 담는 폴더 (줄여서 "repo") |
| **커밋(Commit)** | 변경사항을 하나의 기록으로 확정하는 것 |
| **푸시(Push)** | 내 컴퓨터의 기록을 GitHub 서버로 전송하는 것 |
| **풀(Pull)** | GitHub 서버의 최신 내용을 내 컴퓨터로 가져오는 것 |

---

## 2. 처음 시작할 때 (딱 한 번만)

```bash
# 이 폴더를 git 저장소로 초기화
git init

# GitHub 저장소와 연결 (본인 계정/저장소명으로 변경)
git remote add origin https://github.com/사용자명/저장소명.git
```

이미 GitHub에서 저장소를 만들고 `git clone`으로 받아온 경우라면 이 단계는 생략해도 됩니다.

---

## 3. 파일/폴더 만들 때마다 반복하는 흐름

```bash
# 1) 변경사항을 올릴 준비 (전체 파일: .)
git add .

# 2) 변경사항을 기록으로 확정 (메시지는 무엇을 했는지 간단히 작성)
git commit -m "주간보고서 앱 추가"

# 3) GitHub 서버로 전송
git push
```

> 💡 **팁**: `git add .` 대신 `git add 파일명`으로 특정 파일만 선택해서 올릴 수도 있어요.

---

## 4. 폴더 만드는 법

Git은 **빈 폴더를 저장하지 못해요.** 폴더를 만들려면 그 안에 파일을 최소 하나는 넣어야 GitHub에 반영됩니다.

```
project/
├── docs/
│   └── guide.md      ← 이 파일이 있어야 docs 폴더가 GitHub에 보임
└── main.py
```

GitHub 웹사이트에서 만들 때는 파일명 입력란에 `docs/guide.md`처럼 **슬래시(/)**를 넣으면 폴더가 자동으로 생성됩니다.

---

## 5. `.gitignore`란?

**Git이 추적(관리)하지 않아도 되는 파일 목록**을 적어두는 파일이에요. 예를 들어:

```
# 가상환경 폴더는 용량이 크고 사람마다 달라서 제외
.venv/
.fifthvenv/

# API 키가 들어있는 파일은 절대 올리면 안 됨
.env

# 파이썬 캐시 파일
__pycache__/
*.pyc
```

> ⚠️ **중요**: API 키, 비밀번호가 담긴 파일은 반드시 `.gitignore`에 추가해서 실수로 GitHub에 올라가지 않도록 하세요.

---

## 6. 자주 겪는 문제와 해결법

### ❌ `streamlit run 파일명.py` → "File does not exist"
- **원인**: 터미널의 현재 폴더와 파일이 실제로 있는 폴더가 다름
- **해결**: `dir`(윈도우) 또는 `ls`(맥/리눅스)로 현재 폴더의 파일 목록을 먼저 확인

### ❌ 파일이 탐색기엔 보이는데 터미널은 못 찾음
- **원인**: 파일이 엉뚱한 하위 폴더(예: 가상환경 폴더 안)에 저장되어 있을 수 있음
- **해결**: 파일 탭 우클릭 → `Reveal in File Explorer`로 실제 위치 확인

### ❌ 파이썬 코드를 터미널에 쳤더니 `'from'은(는) 내부 명령이 아닙니다`
- **원인**: 파이썬 코드는 `.py` 파일 안에 작성해야 하는데, 터미널(cmd)에 직접 입력함
- **해결**: 코드는 파일 안에, 터미널에는 `python 파일명.py`처럼 실행 명령만 입력

### ❌ 같은 이름의 파일이 탭에 "파일명 1", "파일명 2"로 뜸
- **원인**: 서로 다른 폴더에 있는 동명 파일이 동시에 열려있음
- **해결**: 한 번에 하나의 프로젝트 폴더만 열어서 작업 (파일 > 폴더 열기)

---

## 7. 유용한 VS Code 단축키 모음

| 단축키 | 기능 |
|---|---|
| `Ctrl + S` | 파일 저장 |
| `Ctrl + `` ` | 터미널 열기/닫기 |
| `Ctrl + Shift + V` | 마크다운 미리보기 |
| `Ctrl + K` → `V` | 마크다운 양쪽 나란히 보기 |
| `Ctrl + K` → `W` | 열려있는 모든 탭 닫기 |
| `Ctrl + F5` | 파이썬 파일 바로 실행 (디버그 없이) |

---

## 8. 체크리스트 (커밋 전에 확인)

- [ ] `dir` 또는 `ls`로 파일이 올바른 위치에 있는지 확인했는가?
- [ ] API 키나 비밀번호가 코드에 그대로 남아있지 않은가?
- [ ] `.gitignore`에 가상환경 폴더가 포함되어 있는가?
- [ ] 커밋 메시지에 "무엇을 했는지"가 잘 드러나는가?

---

*최종 수정일: 2026-09-08*
